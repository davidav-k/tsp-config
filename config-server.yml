server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/yourusername/tsp-config.git
          default-label: main
          clone-on-start: true
          force-pull: true
