spring:
  application:
    name: tsp-config
  cloud:
    config:
      allow-override: true
      override-system-properties: false

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

management:
  endpoints:
    web:
      exposure:
        include: '*'
